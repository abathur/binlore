# notes:
# - this technically targets mount-1003.1-2008

# $1 = verdict
# $2 = executable
execer(){
	case "$2" in
		mount)
			printf "cannot"
			;;
		*)
			echo error, bill bobbinson! 1>&2
			exit 2
			;;
	esac
}

