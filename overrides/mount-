# notes:
# - this technically targets mount-1003.1-2008

execers(){
	cat > $out/execers
} <<EOF
cannot${drv}/bin/mount
EOF

wrappers(){
	cat > $out/wrappers
} <<EOF
EOF

for type in "$@"; do
	$type
done
