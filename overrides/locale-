# notes:
# - this technically targets locale-1003.1-2008

execers(){
	cat > $out/execers
} <<EOF
cannot${drv}/bin/locale
EOF

wrappers(){
	cat > $out/wrappers
} <<EOF
EOF

for type in "$@"; do
	$type
done
